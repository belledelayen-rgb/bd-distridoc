[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Application monopage : toute route inconnue retombe sur index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
